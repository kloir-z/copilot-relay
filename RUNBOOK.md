# RUNBOOK — M365 Copilot を手動コーディングエージェント化する運用手順（v3）

v3 の変更点: プロトコルをコードフェンス依存からセンチネル行方式に変更。
Copilot のコピーボタンは整形済みテキストを返しフェンス行が消えるが、
センチネル行は生き残るので **in.md への手直しが不要になった**。
また3ファイルとも三連バッククォートを含まなくなったため、共有・添付・
リレー経由の転送で入れ子フェンス問題が起きない。

## セットアップ（初回のみ）

1. repoルートに `agent.py` を置く。`agent.config.json` を作り最低限これを設定:

       { "verify": "pytest -q", "auto_commit": true, "paste_limit": 7000 }

   ※ verify は repo のテストコマンドに合わせる。auto_commit を有効にすると
   バッチごとに git コミットが切られ、どのバッチでも `git revert` で巻き戻せる。
   作業は必ず専用ブランチで（`git switch -c agent/xxx`）。
   `.gitignore` に `__pycache__/` 等の生成物を入れておく（ドライバ関連の
   agent.py / in.md / out.txt / resume.txt / agent.config.json は
   チェックポイントから自動除外される）。

2. **Agent Builder が使えるなら（推奨）**: M365 Copilot →「New agent」→
   Configure タブ → Instructions に `BOOTSTRAP.md` の中身を貼って
   個人用エージェントとして保存（instructions 欄は約8000字、現在約5400字で収まる）。
   ※ BOOTSTRAP.md は**意図的に英語**。形式厳守系の指示は英語の方が安定する・
   resume パケット（英語）と一貫する・「英語=作業モード／日本語=依頼主との対話」
   のモード分離がプロトコル脱落（日本語雑談への回帰）を抑えるため。
   ASK と verify 緑後のサマリが日本語なのはルールとして維持されている。
   以後は毎チャットの貼り直し不要。Capabilities の code interpreter は不要
   （Copilot側サンドボックスは使わない）。
   Agent Builder が管理者に無効化されている場合は、新チャット冒頭に
   BOOTSTRAP.md を貼る従来方式で。

3. **モデル選択**: チャット入力欄のモデルスイッチャー（既定 Auto）で、
   Think Deeper 系か、許可されていれば Claude(Opus) / GPT-5.4 Think Deeper を
   明示選択する。diff の正確さ・プロトコル遵守が体感で変わる。
   どのモデルが選べるかはテナントの管理者設定次第。

## 毎ターンの手順（これだけ）

1. Copilot の返信を**メッセージのコピーボタンでコピー**して `in.md` に貼る
   （またはコピーした状態で `python agent.py --clip`）。
   整形済みコピーでフェンスが消えていてもセンチネルは残るので手直し不要。
2. `python agent.py` を実行。
3. `out.txt` ができる。
   - 小さい → そのまま Copilot に貼る。
   - 「LARGE」と表示 → **貼らずに out.txt をファイル添付**する
     （Copilot は TXT 入力を受け付ける。貼り付け上限はテナント/時期で変動し
     信頼できないため、大きいものは常に添付が安全）。
4. 繰り返し。

### あなたの「依頼主ターン」が来る3つの場面

リレー中のあなたは「手」だが、次の場面では**依頼主として**チャットに直接書く:

- **ASK が表示されたとき**: ドライバがコンソールに質問を表示して止まる
  （out.txt は作られない）。回答をそのままチャットに書く。
- **日本語サマリが出たとき**（= verify 緑の単位が完了）: 続行・方針転換・
  終了を指示する。ここが定例の意思決定ポイント。
- **いつでも割り込み**: out.txt を貼る代わりに日本語で指示を書けば、
  エージェントは依頼主の発言として扱う（BOOTSTRAP の「人間の二役」参照）。
  接地情報が古くなるだけなので、次のターンで結果を貼れば復帰する。

依頼主ターンの注意: 日本語の会話往復が増えるとモデルが「普通のチャット」に
引っ張られてプロトコルを忘れやすい。**依頼主として答えるときは短く、末尾に
「プロトコルに戻り、次のペイロードを出して」と一言添える**とドリフトを防げる。

## 仮運用 — M365 Copilot がまだ使えない場合

ドライバはチャットツール非依存。「脳」役は何でもよい:

- **無料版 Copilot（copilot.microsoft.com）**: M365版と同系のUI・レンダラなので、
  「コピーボタンが整形済みテキストを返す」挙動の再現度が最も高い。仮検証の本命。
- **claude.ai / ChatGPT / Gemini**: プロトコル遵守の質が高い。コピーで生Markdownが
  返る（フェンスが残る）ツールもあるが、パーサはどちらでも同じに動く。
- **AI無しの素振り**: in.md にペイロードを手書きして `python agent.py` を実行すれば、
  ドライバ単体の動作確認ができる。

練習用リポジトリはローカルに作る（公開リポジトリには含めていない）。
「バグ入りコード＋落ちるテスト＋タスクを書いた STATE.md ＋ agent.config.json」を
小さな git リポジトリとして用意すればよい。難易度の階段の例:

- `sandbox/` — 初級。単一ファイルのバグ1つ。read → write の1往復を体験する。
- `sandbox2/` — 中級。複数ファイルにまたがる失敗テスト3件＋仕様の矛盾1つ。
  grep/read での調査、diff (apply:) での修正、そして **ASK（依頼主ターン）** が
  実際に発動するところまで体験できる。
- `sandbox3/` — 上級。**ゼロから tkinter GUI アプリを構築**。要件が存在しない
  状態から始まり、最初の ASK で「何を作るか」自体を依頼主に聞いてくる。
  ロジック/UI 分離、テストファースト、そして GUI の目視確認
  （依頼主が `python app.py` を手動起動して見た目を言葉で報告＝人間が「目」になる）
  まで練習できる。run: で GUI を起動させないこと（タイムアウトまで固まる）。

手順:

1. `cd sandbox` して `python agent.py resume` → `resume.txt` ができる
2. resume.txt の中身をチャットに貼る（タスクは STATE.md 由来:「test_average を直せ」）
3. 返信をコピー → `in.md` に貼って保存（**UTF-8で保存**。VS Code / メモ帳の既定でOK）
   → `python agent.py` → `out.txt` を貼り返す、を繰り返す
4. verify（`python -m unittest -q`）が緑になり、auto_commit のチェックポイントが
   切られたら1往復完走。コンソールにも `verify GREEN/RED` が毎回出る。
   もう一周したいときは `git checkout <初期コミット> -- calc.py` でバグだけ復活させる
   （`reset --hard` はドライバ agent.py まで古い版に巻き戻すので使わない）

## 作業単位ごとにチャットを新しくする（重要）

長い1本のチャットを引きずらない。Copilot は会話全体を安定して保持しない
（関連部分の検索ベースで応答する）ため、古い指示やプロトコルが「流れる」。
プロトコル脱落（普通のチャット化）が出始めたら世代交代のサイン。
1つの作業単位（テスト緑）が終わったら:

1. `python agent.py resume` → `resume.txt`
2. 新しいチャット（または自作エージェント）に resume.txt を貼る/添付する。

引き継ぎは**二重の記憶**で成り立つ:

- **意図の記憶 = STATE.md**（モデルが write: で維持）: 要件・決定・却下案・残タスク。
- **活動の記録 = git**（ドライバが自動生成）: checkpoint コミットのメッセージに
  そのバッチの ops 要約（`agent: write app.py, apply pricing.py` 等）が入るので、
  `git log --oneline` がそのまま作業ログになる。

resume パケットには STATE.md / git 状態に加えて、**ファイル一覧・直近コミット・
verify の GREEN/RED** が自動で入る。さらに STATE.md が直近5コミットで
更新されていなければ **STALE 警告**が入り、新チャットの最初のバッチで
「実態と突き合わせて STATE.md を直してから続行せよ」と指示される。
バッチ実行時にも、コードだけ変えて STATE.md を放置すると relay notes に
リマインダーが出る。つまり STATE.md の鮮度維持はモデルの善意任せではなく、
ドライバが構造的に圧をかける。

## pptx スキルでデッキを作る場合

1. ローカルに `npm install -g pptxgenjs`、`pip install Pillow`、LibreOffice(soffice)、
   Poppler(pdftoppm) を入れる。スキルの `extract-text` 等をパスで呼べるようにする。
2. Copilot に「まず skills/pptx/SKILL.md を read してから従え。新規は pptxgenjs 経路」
   と指示（READ してから WRITE の徹底）。
3. Copilot が build_deck.js を write → `run: node build_deck.js` で .pptx 生成。
4. 内容QA: `run: extract-text deck.pptx`（+ placeholder grep）。
5. ビジュアルQA: `python agent.py qa deck.pptx` で slide-*.jpg を生成し、
   **新しい Copilot チャットに画像を添付**して出力された検査プロンプトを貼る
   （＝先入観のないサブエージェント検査）。指摘をビルド側に戻し、1サイクルで止める。

## トラブル対応

- **返信が途中で切れた** → ドライバが `-----END-----` の欠落を検知し、
  **何も実行せず**メッセージを出して終了する。Copilot に「CONTINUE」とだけ送る。
  再送（切れたセクションのセンチネル行から）が来たら、in.md の**切れた
  セクション（最後のセンチネル行以降）を削除**してから再送分を末尾に貼り足し、
  再実行する。
- **プロトコル崩れ（OPSセクションがない、ENDだけ無い等）** → ドライバが何も
  実行しないだけ。「フォーマットが崩れている。OPS〜END のペイロード1つで
  出し直して」と返す。
- **プロトコル脱落（普通のチャットに戻り「このコードを貼って」と人間に手編集を
  頼んでくる）** → **手で編集しない**。人間がコードを触ると git・STATE.md と
  モデルの認識がずれ、接地が壊れる。コピペ用の戻し文句:
  「プロトコル崩れ。人間にコード編集はさせない。その変更を write:（全文）か
  apply:（正確な diff）の OPS〜END ペイロード1つで出し直して。」
  戻らない・繰り返す場合は長チャットの劣化が根因なので、STATE.md を更新させて
  から `python agent.py resume` で新しいチャットに移る。
- **パッチ失敗 [PATCH FAILED]** → そのまま貼り戻せばよい。Copilot に
  「対象範囲を read: してから diff を作り直せ」と促すのが早い。
- **ファイル本文にセンチネル風の行やフェンスが含まれる** → BOOTSTRAP の
  ヒアドック形式（`UNTIL:トークン`）を使わせる。逐語で保存される。
- **暴走が怖い** → DANGER パターン（rm -rf 等）は実行前に人間の y/N 確認が入る。
  最終防壁は「実行するのは常に人間」という構造そのもの。

## 会社への説明材料（確認事項）

- 社内コードを Copilot チャットに入れてよいかは、テナントのデータ保護設定と
  社内規程次第。管理者に確認する。参考: Anthropic モデルは Microsoft の
  サブプロセッサとして Product Terms / DPA の下で提供される。
  一部プレビューモデルはプロバイダ側にプロンプトが保持される旨の表示が出る
  ので、そのモデルは避ける。
- 正攻法の代替として Copilot Cowork（タスクを最後まで実行するエージェント、
  使用量課金・管理者がコスト管理可能）が GA 済み。ローカル repo は触れないので
  このリレーの完全な代替ではないが、「公式にエージェントを使わせてほしい」
  という交渉の持ち駒になる。

## 既知の限界（過大評価しないために）

- 真の自走はしない。ループの実体は人間+スクリプト。1返信=1バッチが上限速度。
- Copilot がプロトコルを毎回完璧に守る保証はない。崩れたら1行で叱って戻す。
- 出力上限・貼り付け上限・使えるモデル・コピーボタンの挙動はテナント依存で
  変動する。この RUNBOOK の数値は「あなたの環境で実測して上書きする」前提。
