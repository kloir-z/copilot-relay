# RUNBOOK — M365 Copilot を手動コーディングエージェント化する運用手順（v3）

v3 の変更点: プロトコルをコードフェンス依存からセンチネル行方式に変更した。
Copilot のコピーボタンは整形済みテキストを返しフェンス行が消えるが、
センチネル行は残るため、in.md への貼り付けに手直しが要らない。
また各ファイルとも三連バッククォートを含まないため、共有・添付・
リレー経由の転送で入れ子フェンスの問題が起きない。

## セットアップ（初回のみ）

1. repoルートに `agent.py` を置く。`agent.config.json` を作り最低限これを設定:

       { "verify": "pytest -q", "auto_commit": true, "paste_limit": 7000 }

   verify は repo のテストコマンドに合わせる。auto_commit を有効にすると
   バッチごとに git コミットが作られ、任意のバッチを `git revert` で巻き戻せる。
   作業は専用ブランチで行う（`git switch -c agent/xxx`）。
   `.gitignore` に `__pycache__/` 等の生成物を入れておく（ドライバ関連の
   agent.py / in.md / out.txt / resume.txt / agent.config.json は
   チェックポイントから自動除外される）。

2. Agent Builder が使える場合（推奨）: M365 Copilot →「New agent」→
   Configure タブ → Instructions に `BOOTSTRAP.md` の中身を貼って
   個人用エージェントとして保存する（instructions 欄は約8000字。現在約6100字）。
   以後は毎チャットの貼り直しが不要。Capabilities の code interpreter は不要
   （Copilot 側サンドボックスは使わない）。
   Agent Builder が管理者に無効化されている場合は、新チャット冒頭に
   BOOTSTRAP.md を貼る方式で行う。

   BOOTSTRAP.md は意図的に英語にしている。理由は、形式厳守系の指示は英語の
   方が安定すること、resume パケット（英語）と一貫すること、英語を作業用・
   日本語を依頼主との対話用に分けるとプロトコル忘れ（雑談への回帰）が
   起きにくくなること。ASK と verify 通過後のサマリを日本語で書く規則は
   BOOTSTRAP 内に維持されている。

3. モデル選択: チャット入力欄のモデルスイッチャー（既定 Auto）で、
   Think Deeper 系か、許可されていれば Claude(Opus) / GPT-5.4 Think Deeper を
   明示選択する。diff の正確さとプロトコル遵守に差が出る。
   選択できるモデルはテナントの管理者設定による。

## 毎ターンの手順

1. Copilot の返信をメッセージのコピーボタンでコピーし、`in.md` に貼る
   （またはコピーした状態で `python agent.py --clip`）。
   整形済みコピーでフェンスが消えていてもセンチネルは残るため手直し不要。
2. `python agent.py` を実行する。
3. `out.txt` ができる。
   - 小さい場合はそのまま Copilot に貼る。
   - 「LARGE」と表示された場合は貼らずに out.txt をファイル添付する
     （Copilot は TXT 入力を受け付ける。貼り付け上限はテナントや時期で
     変動するため、大きいものは添付が確実）。
4. 繰り返す。

### 依頼主としてチャットに直接書く場面

リレー作業中は実行と貼り付けに徹するが、次の場面では依頼主として直接書く:

- ASK が表示されたとき: ドライバがコンソールに質問を表示して止まる
  （out.txt は作られない）。回答をそのままチャットに書く。これが正規の
  手順であり、チャット欄に書くこと自体に問題はない。回答は短くし、
  末尾に「プロトコルに戻り、次のペイロードを出して」と添える。
- 日本語サマリが出たとき（verify 通過の単位が完了）: 続行・方針転換・
  終了を指示する。
- 任意のタイミングの割り込み: out.txt を貼る代わりに日本語で指示を書けば、
  エージェントは依頼主の発言として扱う（BOOTSTRAP の「人間の二役」参照）。
  次のターンで結果を貼れば通常の流れに戻る。

注意: 日本語の会話往復が増えるとモデルがプロトコルを忘れやすくなる。
依頼主として答えるときは短くし、末尾に「プロトコルに戻り、次のペイロードを
出して」と添えるとよい。

## 仮運用 — M365 Copilot がまだ使えない場合

ドライバはチャットツールに依存しないため、相手は何でもよい:

- 無料版 Copilot（copilot.microsoft.com）: M365 版と同系の UI・レンダラのため、
  コピーボタンの挙動の再現度が最も高い。
- claude.ai / ChatGPT / Gemini: コピーで生 Markdown が返る（フェンスが残る）
  ツールもあるが、パーサはどちらの形式でも動く。
- AI を使わない確認: in.md にペイロードを手書きして `python agent.py` を
  実行すれば、ドライバ単体の動作確認ができる。
- ローカルモデル（Ollama / 自作チャットアプリ等）: thinking（推論トレース）を
  応答と分離しないアプリだと、思考過程がそのまま返信になりペイロードが
  出ないことがある（カラム0センチネルが無いためパーサは何も実行しない）。
  アプリ側で thinking を非表示にするかモデルの thinking モードを無効にする。
  プロトコルには <scratch> という思考チャネルが既にあるため、モデル側の
  thinking は切ってよい。小規模モデルはフォーマット遵守が甘く修復の往復が
  増えるので、生成の max tokens を多めに取り（END 欠落の切断を減らす）、
  温度は低めにする。

練習用リポジトリはローカルに作る（このリポジトリには含めていない）。
「バグ入りコード＋落ちるテスト＋タスクを書いた STATE.md ＋ agent.config.json」を
小さな git リポジトリとして用意する。難易度別の例:

- 初級: 単一ファイルのバグ1つ。read → write の1往復を確認する。
- 中級: 複数ファイルにまたがる失敗テスト数件＋仕様の矛盾1つ。grep/read での
  調査、diff (apply:) での修正、ASK の発動までを確認できる。
- 上級: tkinter GUI アプリをゼロから構築する。要件が未定の状態から始め、
  最初の ASK で作るものを決める。ロジック/UI 分離、テストファースト、
  GUI の動作確認（依頼主が `python app.py` を手動起動し、見た目・動作を
  文章で報告する）までを練習できる。run: で GUI を起動させないこと
  （ウィンドウが開くとタイムアウトまで停止する）。

手順:

1. 練習用リポジトリで `python agent.py resume` を実行し `resume.txt` を作る
2. resume.txt の中身をチャットに貼る（タスクは STATE.md から引き継がれる）
3. 返信をコピー → `in.md` に貼って保存（UTF-8 で保存。VS Code / メモ帳の
   既定で問題ない）→ `python agent.py` → `out.txt` を貼り返す、を繰り返す
4. verify が通り、auto_commit のチェックポイントが作られたら1往復完了。
   コンソールにも `verify GREEN/RED` が毎回表示される。
   やり直すときは `git checkout <初期コミット> -- 対象ファイル` でバグだけ
   復活させる（`reset --hard` はドライバ agent.py まで古い版に戻るため
   使わない）

## 作業単位ごとにチャットを新しくする

長い1本のチャットを引きずらない。Copilot は会話全体を安定して保持しない
（関連部分の検索ベースで応答する）ため、古い指示やプロトコルが効かなくなって
いく。プロトコル忘れ（普通のチャット化）が出始めたら作り直しの目安。
1つの作業単位（テスト通過）が終わったら:

1. `python agent.py resume` → `resume.txt`
2. 新しいチャット（または自作エージェント）に resume.txt を貼る/添付する。

引き継ぎに使う情報は2種類ある:

- STATE.md（モデルが write: で維持する）: 要件・決定・却下案・残タスク。
- git 履歴（ドライバが自動記録する）: checkpoint コミットのメッセージに
  そのバッチの ops 要約（`agent: write app.py, apply pricing.py` 等）が
  入るため、`git log --oneline` が作業ログとして読める。

resume パケットには STATE.md と git 状態に加えて、ファイル一覧・直近コミット・
verify の GREEN/RED が自動で入る。STATE.md が直近5コミットで更新されていない
場合は STALE 警告が入り、新チャットの最初のバッチで実態と突き合わせて
STATE.md を更新するよう指示される。バッチ実行時にも、コードだけ変えて
STATE.md を更新しなかった場合は relay notes にリマインダーが出る。
STATE.md の鮮度維持はモデル任せにせず、ドライバ側でも検知して催促する。

### 作業単位の途中でもチャットを捨ててよい

out.txt には毎ターン STATE.md・git 状態・直近コミット5件が入るため、
チャットはいつ捨てても、直近の out.txt か resume.txt を新チャットに
貼れば再開できる。クリアで失われるのはモデルの思考途中（scratch）だけで、
その復元は STATE.md の「次の一手」と再 read の1〜2往復で済む。

これを支えるのが STATE.md の規律で、ドライバ側でも機械チェックする:

- STATE.md が無い / 「次の一手」セクションが無い → relay notes で警告。
  「次の一手」は新チャットがそのまま実行できる具体度で常に維持させる。
- 行数超過（既定60行、`state_max_lines` で変更） → 縮めるよう警告。
  STATE.md は毎ターン out.txt に全文載るため、肥大は毎ターンの固定費になる。

コンテキストの短いモデル（ローカル LLM 等）では、この仕組みを前提に
「N ターンごとに機械的に新チャットへ移る」運用にしてよい。

## トラブル対応

- 返信が途中で切れた → ドライバが `-----END-----` の欠落を検知し、何も
  実行せずメッセージを出して終了する。Copilot に「CONTINUE」とだけ送る。
  再送（切れたセクションのセンチネル行から）が来たら、in.md の切れた
  セクション（最後のセンチネル行以降）を削除してから再送分を末尾に貼り足し、
  再実行する。
- プロトコル崩れ（OPS セクションがない、END だけ無い等） → ドライバが何も
  実行しないだけ。「フォーマットが崩れている。OPS〜END のペイロード1つで
  出し直して」と返す。
- プロトコル忘れ（普通のチャットに戻り「このコードを貼って」と人間に手編集を
  頼んでくる） → 手で編集しない。人間がコードを触ると git・STATE.md と
  モデルの認識がずれ、以後の判断の前提が壊れる。返信例:
  「プロトコル崩れ。人間にコード編集はさせない。その変更を write:（全文）か
  apply:（正確な diff）の OPS〜END ペイロード1つで出し直して。」
  戻らない・繰り返す場合は長いチャットの劣化が原因なので、STATE.md を
  更新させてから `python agent.py resume` で新しいチャットに移る。
- パッチ失敗 [PATCH FAILED] → そのまま貼り戻せばよい。Copilot に
  「対象範囲を read: してから diff を作り直せ」と促すと早い。
- ファイル本文にセンチネル風の行やフェンスが含まれる → BOOTSTRAP の
  ヒアドック形式（`UNTIL:トークン`）を使わせる。本文は逐語で保存される。
- 破壊的コマンドの抑止 → DANGER パターン（rm -rf 等）は実行前に人間の
  y/N 確認が入る。また実行は常に人間が行うため、実行前に止める機会が必ずある。

## 既知の限界

- 真の自走はしない。ループの実体は人間とスクリプトで、1返信=1バッチが
  速度の上限になる。
- Copilot がプロトコルを毎回守る保証はない。崩れたら短く指摘して戻す。
- 出力上限・貼り付け上限・使えるモデル・コピーボタンの挙動はテナント依存で
  変動する。本書の記述は各自の環境で実測して上書きする前提。
